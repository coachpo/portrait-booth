# O3: Dockerfile line 5 runs npm ci to install dependencies first; line 6's
# COPY frontend/ must not let the dev machine's node_modules/dist override the
# build output — "syncing assets from node_modules at build time" only makes
# sense inside the image
.git
frontend/node_modules
frontend/dist
backend/.venv
backend/data
__pycache__
.pytest_cache
.ruff_cache
*.db
*.sqlite3
