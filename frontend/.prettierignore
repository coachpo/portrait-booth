# Third-party build artifacts: formatting would change their bytes, directly
# conflicting with SPEC §4.4 "audit-locked build".
# prettier once stripped a trailing newline from the MediaPipe WASM glue here,
# making the self-hosted copy no longer byte-identical to the npm package and
# voiding the version-lock basis.
public/assets/models/

dist/
coverage/
