# 项目状态

> 最近一次基于仓库证据的评审日期：2026-08-06

## 生命周期

Portrait Booth 处于首个 MVP 实现阶段。产品与技术规格（[产品说明](docs/PRODUCT.md)、[SPEC](docs/SPEC.md)）已完成，P0 取回策略已选定（`key_only_ephemeral`，暂存 TTL 30 天，见 SPEC §1.2.1）；仓库正在从纯文档脚手架过渡到可运行应用。

## 当前实现

- 仓库已建立 monorepo 工具链：`frontend/`（Vite + React + TypeScript）、`backend/`（FastAPI + SQLite + 本地磁盘存储）、`templates/`（版本化模板数据）。
- 创建流程已实现：模板选择向导（TMP-001~005）→ 上传/摄像头拍摄（SRC-001~005、CAM-001~008）→ MediaPipe 姿态指导与静态复检（GDE-001~010）→ 非破坏性编辑（EDT-001~009）→ 终态渲染与检查摘要（OUT-001~008）。
- 暂存/取回已实现：匿名保存会话、幂等暂存、KEY-only 取回、单次下载 token、删除密钥授权（SPEC §6.2~§6.5，后端 47 项 pytest + 前端 145 项测试覆盖）。
- 证件照片模板是 [SPEC §5.2](docs/SPEC.md) 候选内容的实现版本；在官方来源复核和发布验收完成前，不代表可用模板或政府认可。
- 部署尚未上线；`docker compose` 与 Dockerfile 仅用于本地开发与验收。
- 已知后续项：MediaPipe 推理当前在主线程运行（Worker 迁移为后续增强，见 SPEC §4.4）；姿态/曝光检查为启发式并明确标注未官方校准。

## 数据与兼容性

- 仓库本身不包含用户照片、运行时数据库或应用生成数据。
- 当前没有经过生产验证的浏览器、API、配置或存储兼容性承诺；目标浏览器与生命周期约束按 [SPEC](docs/SPEC.md) 实现草案执行。

## 允许的变更

- 依据[产品说明](docs/PRODUCT.md)和[SPEC](docs/SPEC.md)实施与修正应用实现。
- 维护首个应用工具链、运行时代码和自动化测试，并同步记录稳定命令。
- 在不削弱隐私、安全、模板可追溯性和输出一致性的前提下实现已确认需求。

## 禁止的变更

- 在没有实现和验证证据时，把规划功能描述为已发布或可用。
- 把启发式照片检查描述为政府批准、官方认证或必然获批。
- 提交凭据、私密照片、个人数据、本地环境文件或无法安全公开的测试素材。
- 未经验证就放宽照片暂存、KEY 取回、日志、缓存或删除方面的安全约束。

## 再次评审条件

引入外部用户、生产部署、不可丢弃数据、公开 API 或浏览器兼容承诺时，必须重新评审本文件。
